return {
  default = {
    opts = {
      cursorline = false,
      tab_width = 2,
      use_ripgrep = true,
      scrolloff = 2,
      treesitter_folds = false,
      load_plugins = { },
    },
  },
}
